name: inforsa_inventory
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.10.8 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  flutter_dotenv: ^5.1.0
  provider: ^6.1.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  # VERSI 6.0.0 TERLALU TINGGI, PAKAI 2.0.0 AGAR COCOK DENGAN SDK 3.10
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - "assets/logo inforsa.png"
